计网软件
1.做个ping客户端，可以ping端口
2.做一个套接字监听器，可以监听某个ip下的端口，接受其返回（发送）的消息（定时监听，持续监听，短线后重连，接受返回的消息，回复消息）
3.做一个HTTP连接器，去判断某个URL是否可用。
4.SSL
5.如何实现别人建立socket连接，把发送的数据都收集起来

应用程序：
1.做一个聊天室
2.做一个Web服务器（tomcat是怎么写出来的）、普通服务器
