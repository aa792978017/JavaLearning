迭代器模式：

    定义：提供一种方法访问一个容器对象中的各个元素，而又不需要暴露该对象的内部细节

    角色：
        1、抽象迭代器：负责定义访问和遍历元素的接口，而且基本有两个固定的方法，next(),hasNext()
        2、具体迭代器：具体迭代器要实现的抽象迭代器的接口，完成容器元素的遍历
        3、抽象容器：容器角色负责提供一个创建具体迭代器角色的接口
        4、具体容器：实现容器接口定义的方法

    场景：为容器服务，目前一般不需要我们自己去写，一般用JDK里面的即可